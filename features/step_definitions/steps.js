"use strict";

var steps = function steps(){
    var fs = require('fs');

    var wdav = require('../../lib/webdav.js')({
        username: 'foo',
        password: 'bar'
    });

    this.Given(/^a file on "([^"]+)"/, function(locale, callback) {

    }
}
